# Non-secret Firebase Functions runtime configuration.
# Copy to .env.dottodot-7ab01 for this Firebase project.
SITE_URL=https://dottodotfreeprintables.com

# Use the Stripe test-mode price ID while testing, then replace it with the
# live-mode price ID only when STRIPE_SECRET_KEY is also switched to live mode.
STRIPE_PRICE_PREMIUM_PUZZLE_PACK=price_replace_me
